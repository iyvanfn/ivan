CREATE OR REPLACE VIEW exam_marks AS
    SELECT                                      FROM
    JOIN         ON
    JOIN      ON
    JOIN            ON
    WHERE                                          = 'Nathanial Lyman'
    ORDER BY        ;
SELECT * FROM exam_marks;